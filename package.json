{
  "name": "airdraw",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'build stub'",
    "dev": "echo 'dev stub'",
    "test": "vitest run",
    "package": "echo 'electron package stub'"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^0.34.6",
    "jsdom": "^26.1.0"
  }
}
